# -*- coding: utf8 -*-
from distutils.core import setup

# Required packages are included in this list
# required_packages = ["clean-text==0.6.0"]

setup(
    name="pkg",
    version="0.1",
    description="Library to facilitate the cascade reconstruction project",
    author="M.R. DeVerna and F. Pierri",
    packages=["pkg"],
    # install_requires=required_packages,
)
