# -*- coding: utf8 -*-
from distutils.core import setup

# Required packages are included in this list
# required_packages = []

setup(
    name="midterm",
    version="0.1",
    description="Local package for the midterm portion of this project",
    author="Matthew DeVerna",
    packages=["midterm"],
    # install_requires=required_packages,
)
